# Legacy Snapshot

This folder contains the original rehydrated artifacts from `dependencies-gen.patch` (Code.org-oriented outputs and link validators).

They are archived for provenance and are not used by the standalone CAG example flow.
